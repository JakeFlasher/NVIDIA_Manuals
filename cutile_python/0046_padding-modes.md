---
title: "Padding Modes"
section: ""
source: "https://docs.nvidia.com/cuda/cutile-python/data.html#padding-modes"
---

## [Padding Modes](https://docs.nvidia.com/cuda/cutile-python#padding-modes)[](https://docs.nvidia.com/cuda/cutile-python/#padding-modes "Permalink to this headline")

```
_`class`_`cuda.tile.``PaddingMode`[](https://docs.nvidia.com/cuda/cutile-python/#cuda.tile.PaddingMode "Link to this definition")
```

Padding mode for load operation.

```
`UNDETERMINED`_`=` `'undetermined'`_[](https://docs.nvidia.com/cuda/cutile-python/#cuda.tile.PaddingMode.UNDETERMINED "Link to this definition")
```

The padding value is not determined.

```
`ZERO`_`=` `'zero'`_[](https://docs.nvidia.com/cuda/cutile-python/#cuda.tile.PaddingMode.ZERO "Link to this definition")
```

The padding value is zero.

```
`NEG_ZERO`_`=` `'neg_zero'`_[](https://docs.nvidia.com/cuda/cutile-python/#cuda.tile.PaddingMode.NEG_ZERO "Link to this definition")
```

The padding value is negative zero.

```
`NAN`_`=` `'nan'`_[](https://docs.nvidia.com/cuda/cutile-python/#cuda.tile.PaddingMode.NAN "Link to this definition")
```

The padding value is NaN.

```
`POS_INF`_`=` `'pos_inf'`_[](https://docs.nvidia.com/cuda/cutile-python/#cuda.tile.PaddingMode.POS_INF "Link to this definition")
```

The padding value is positive infinity.

```
`NEG_INF`_`=` `'neg_inf'`_[](https://docs.nvidia.com/cuda/cutile-python/#cuda.tile.PaddingMode.NEG_INF "Link to this definition")
```

The padding value is negative infinity.
