---
title: "4.4.5.1. Sync"
section: "4.4.5.1"
source: "https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cooperative-groups.html#sync"
---

### [4.4.5.1. Sync](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics#sync)[](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/#sync "Permalink to this headline")

You can synchronize a group by calling the collective `sync()` function. Like `__syncthreads()`, the `sync()` function makes the following guarantees:
- All memory accesses (e.g., reads and writes) made by threads in the group before the synchronization point are visible to all threads in the group after the synchronization point.
- All threads in the group reach the synchronization point before any thread is allowed to proceed beyond it.

The following example shows a `cooperative_groups::sync()` that is equivalent to `__syncthreads()`.

```c++
namespace cg = cooperative_groups;

cg::thread_block my_group = cg::this_thread_block();

// Synchronize threads in the block
cg::sync(my_group);
```

Cooperative groups can be used to synchronize the entire grid. As of CUDA 13, cooperative groups can no longer be used for multi-device synchronization.
For details see the [Large Scale Groups](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/#cooperative-groups-large-scale-groups) section.

More information about synchronization is available in the [Cooperative Groups API](https://docs.nvidia.com/cuda/cuda-programming-guide/05-appendices/device-callable-apis.html#cg-api-sync-header).
